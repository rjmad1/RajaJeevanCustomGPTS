{
  "name": "ai-agent-portal-root",
  "private": true,
  "scripts": {
    "bootstrap": "npm install",
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web"
  },
  "workspaces": [
    "apps/web"
  ]
}
